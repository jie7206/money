<span id="site_title">
    <h1 style="width: <%= $default_table_width %>px">
      <%= site_logo %>
      <%= link_to get_huobi_acc_id, $host2 %>
      <% if admin? %>
        <a href="<%= root_path %>"><%= site_name %></a>
        <%= link_to raw($site_slogon), "#{$host2}deal_records" %>
      <% end %>
    </h1>
</span>
<%= render 'layouts/site_nav', id: 'site_nav' %>
