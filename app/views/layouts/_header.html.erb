<span id="site_title">
  <a href="<%= root_path %>">
    <h1><%= site_logo %><%= site_name %></h1>
  </a>
</span>
<% if login? %>
  <nav id="site_nav">
    <small>
      <%= link_back_to_properties %> |
    <% if admin? %>
      <%= link_to t(:properties_cloud), properties_cloud_path %> |
    <% end %>
      <%= link_to t(:currency_index), currencies_path %> |
      <%= link_to t(:interest_index), interests_path %> |
      <%= link_to t(:item_index), items_path %> |
      <%= link_to t(:logout), logout_path, { id: 'logout' } %>
    </small>
  </nav>
<% end %>
